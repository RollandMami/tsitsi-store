# Si python est >= 3.9 install la version requis sinon install un par defqut
asgiref==3.11.0 ; python_version >= "3.9"
asgiref>=3.0.0 ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
Django==5.2.9 ; python_version >= "3.9"
Django ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
charset-normalizer==3.4.4 ; python_version >= "3.9"
charset-normalizer ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-appconf==1.2.0 ; python_version >= "3.9"
django-appconf ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-cleanup==9.0.0 ; python_version >= "3.9"
django-cleanup ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-humanize==0.1.2 ; python_version >= "3.9"
django-humanize ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-mathfilters==1.0.0 ; python_version >= "3.9"
django-mathfilters ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-resized==1.0.3 ; python_version >= "3.9"
django-resized ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django-restframework==0.0.1 ; python_version >= "3.9"
django-restframework ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
django_compressor==4.6.0 ; python_version >= "3.9"
django_compressor ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
et_xmlfile==2.0.0 ; python_version >= "3.9"
et_xmlfile ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
humanize==4.15.0 ; python_version >= "3.9"
humanize ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
openpyxl==3.1.5 ; python_version >= "3.9"
openpyxl ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
pillow==12.0.0 ; python_version >= "3.9"
pillow ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
rcssmin==1.2.2 ; python_version >= "3.9"
rcssmin ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
reportlab==4.4.7 ; python_version >= "3.9"
reportlab ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
rjsmin==1.2.5 ; python_version >= "3.9"
rjsmin ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
sqlparse==0.5.5 ; python_version >= "3.9"
sqlparse ; python_version < "3.9"

# Si python est >= 3.9 install la version requis sinon install un par defqut
typing_extensions==4.15.0 ; python_version >= "3.9"
typing_extensions ; python_version < "3.9"

djangorestframework